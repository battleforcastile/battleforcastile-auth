{{- if .Values.ingress.enabled -}}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-battleforcastile-auth-ingress
spec:
  backend:
    serviceName: {{ .Release.Name }}-battleforcastile-auth-service
    servicePort: {{ .Values.ingress.port }}

{{- end }}
